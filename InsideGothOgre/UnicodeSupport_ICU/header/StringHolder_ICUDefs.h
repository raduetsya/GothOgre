#ifndef STRING_HOLDER_ICU_DEFS_H
#define STRING_HOLDER_ICU_DEFS_H

namespace GothOgre
{
	// Initial typedefs
	typedef int32_t  size_type; // type for string's length and indices
	typedef uint8_t  char8;     // 8-bits char's type
	typedef char     char8a;    // alternative 8-bits char's type
	typedef uint16_t char16;    // 16-bits char's type
	typedef UChar    char16a;   // alternative 16-bits char's type
}

#endif // STRING_HOLDER_ICU_DEFS_H