#ifndef STRING_HOLDER_WIN_DEFS_H
#define STRING_HOLDER_WIN_DEFS_H

namespace GothOgre
{
	// Initial typedefs
	typedef size_t size_type; // type for string's length and indices
	typedef uint8  char8;     // 8-bits char's type
	typedef CHAR   char8a;    // alternative 8-bits char's type
	typedef uint16 char16;    // 16-bits char's type
	typedef WCHAR  char16a;   // alternative 16-bits char's type
}

#endif // STRING_HOLDER_WIN_DEFS_H